# NanoClaw Qwen 代理集成部署指南

本文档记录了基于“API 代理网关（方案一）”将阿里云 Qwen 接入 NanoClaw 的实现逻辑、涉及修改的代码，以及在 Ubuntu 服务器上的完整部署执行步骤。

## 1. 架构与改动总结

> [!TIP]
> **设计原理**：保留强大且自带近 20 种工具（如执行 Bash、文件读写等）的官方 `@anthropic-ai/claude-agent-sdk` 作为核心引擎。通过部署一个本地的 LiteLLM 代理，将 SDK 发出的 Anthropic API 格式流量“无损翻译”为 OpenAI 兼容格式，发给阿里云百炼平台（Qwen 模型）。

我们为你完成了如下改动（对应社区的 PR #672）：

### 修改了宿主机配置注入
为了使 NanoClaw 能够灵活动态地选用代理后的模型名，而不是在引擎里写死使用 Claude 3.5 Sonnet：
- **[src/container-runner.ts](file:///d:/CodeSpace/nanoclaw/src/container-runner.ts)**
  - **修改了** [readSecrets()](file:///d:/CodeSpace/nanoclaw/src/container-runner.ts#213-225) 函数，使宿主机在启动容器时，能从 `.env` 中正确读取并向容器内注入 `ANTHROPIC_MODEL` 环境变量。

### 修改了容器内的 SDK 调用
- **[container/agent-runner/src/index.ts](file:///d:/CodeSpace/nanoclaw/container/agent-runner/src/index.ts)**
  - **修改了** `query({ options })` 初始化参数。现在，SDK 向代理服务器发起请求时，将不再强制使用默认模型，而是读取 `sdkEnv.ANTHROPIC_MODEL`，这使得流量能够被 LiteLLM 路由配置优雅地匹配并转发到 Qwen。

### 新增了代理部署资产
我们在根目录下新增了两个用于快速启动代理的文件：
- **[NEW] [litellm_config.yaml](file:///d:/CodeSpace/litellm_config.yaml)**：LiteLLM 路由配置文件，将接收到的 `claude-3-5-sonnet-20241022` 映射到 `openai/qwen-plus` 终点，并在发包时附加 `"enable_thinking": true` 开启深度思考。
- **[NEW] [start_litellm.sh](file:///d:/CodeSpace/start_litellm.sh)**：一键部署代理服务器的 Ubuntu 启动脚本。

---

## 2. Ubuntu 部署执行步骤

请在你的 Ubuntu 服务器上依次执行以下步骤：

### 第一步：获取最新代码并解决依赖

首先，拉取我们刚刚推送到 Remote [main](file:///d:/CodeSpace/nanoclaw/container/agent-runner/src/index.ts#494-588) 分支的修改，并因为修改了 TypeScript 文件，所以需要重新编译构建。

```bash
# 1. 进入 NanoClaw 项目根目录
cd /path/to/nanoclaw

# 2. 拉取包含 Qwen 支持的最新代码
git pull origin main

# 3. 安装依赖并重新编译（在 Ubuntu 下运行避免了刚才你在 Window 遇到的权限问题）
npm install
npm run build
```

### 第二步：启动本地 LiteLLM 代理网关

我们需要使用 Docker 在后台启动 LiteLLM 容器。

```bash
# 1. 赋予启动脚本执行权限
chmod +x start_litellm.sh

# 2. 执行启动脚本
./start_litellm.sh
```

> [!NOTE]
> 运行脚本后，它会要求你在终端输入你的**阿里云百炼 API Key (`sk-...`)**。
> 提供后，脚本会自动拉取 `berriai/litellm` 镜像，并将代理服务映射到物理机的 **4000** 端口。

### 第三步：修改 NanoClaw 环境变量

在 NanoClaw 的根目录中找到或创建 `.env` 文件，加入以下代理相关的环境变量（覆盖原有配置）：

```env
# 指定 Anthropic SDK 将请求发送给我们的本地 LiteLLM 代理
ANTHROPIC_BASE_URL=http://<你服务器的局域网或公网IP>:4000

# 占位符 API Key，由于代理不验证此 Key，这里只需填入任意字符串即可
ANTHROPIC_API_KEY=sk-proxy-dummy-key

# 指定我们配置文件中注册了路由规则的模型名称
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022
```

### 第四步：启动 NanoClaw

如同平常一样启动 NanoClaw 的主服务。

```bash
npm start
```

---

## 3. 验证功能

启动成功后，你可以通过飞书频道或者 CLI 向 NanoClaw 发送指令验证：

1. **测试工具调用**：发送类似 `“请帮我查看当前目录下有哪些文件”`。
   - 预期结果：即使使用的是 Qwen，它依然能完美地调用底层的 [Bash](file:///d:/CodeSpace/nanoclaw/container/agent-runner/src/index.ts#193-211) 或 `Glob` 工具并返回结果。
2. **测试深度思考**：发送一道逻辑题。
   - 预期结果：你可以通过 `docker logs -f nanoclaw-litellm-proxy` 看到 LiteLLM 内部向阿里云发起了带 `enable_thinking` 参数的请求。

如果有任何环节出现失败（比如工具调用报错等），可以通过查看代理日志 `docker logs nanoclaw-litellm-proxy` 来追踪报文格式是否正确转换。
